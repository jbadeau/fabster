# nx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx` to build the library.

## Running unit tests

Run `nx test nx` to execute the unit tests via [Vitest](https://vitest.dev/).
