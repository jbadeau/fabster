# Initialize an Nx workspace named `name`

**Outputs:** `repo` (uri) — location of the initialized workspace

Scaffold a fresh Nx workspace and commit the baseline.

* Initialize an Nx workspace (no Nx Cloud)
* Set the package scope to `name`
* Commit the baseline
