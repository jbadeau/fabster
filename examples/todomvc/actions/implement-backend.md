# Implement the Express backend from `spec` into `app`

**Outputs:** _none_

Read the API contract and implement a working Express server.

* Read the OpenAPI contract `spec`
* Implement CRUD routes with validation and error handling
* Use in-memory storage and correct HTTP status codes
* Build the `app` project
