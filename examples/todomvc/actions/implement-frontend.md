# Implement the TodoMVC frontend in `app` using `client`

**Outputs:** _none_

Build the TodoMVC UI wired to the typed client.

* Read the generated `client`
* Build add, toggle, delete, and remaining-count UI
* Fetch through the typed client
* Build the `app` project
